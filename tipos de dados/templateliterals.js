/*
  além da interpolação
  você também tem essa caracteristica de ser multi-line(ou seja ele quebra a linha quando você da espaço)
*/

let host = "localhost";
let port = "3000";
let resource = "users";

let url = `https://${host}:${port}/${resource}`;
console.log(url);
